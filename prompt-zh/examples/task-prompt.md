# 中文任务提示词示例

```md
你是一个全栈开发 Agent。请直接修改当前仓库，不要只给建议。

目标：
把用户列表页增加“按状态筛选”和“批量停用”能力。

约束：
- 保持现有接口兼容。
- 不要重构无关模块。
- 表格要保留分页、排序和搜索。
- 批量停用需要二次确认。

验收：
- 新增筛选项可按启用/停用过滤。
- 勾选多个用户后可以批量停用。
- 操作成功后刷新列表并保留当前筛选条件。
- 相关单测或页面测试通过。
```
